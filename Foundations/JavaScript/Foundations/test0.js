/**function add7(n) {
    return (n + 7)
}
console.log (add7(5))
*/

/** 
function multiply(x,y) {
    c = (x * y)
    return (c)
}
console.log (multiply(5,10))
*/

/**
const str = 'strIng!';
const strCorrected = str.toLowerCase()
const strFinal = strCorrected.charAt(0).toUpperCase() + strCorrected.slice(1);
console.log(strFinal);
*/

/** 
function lastLetter(str) {
    const last = str.slice(-1)
    console.log(last)
}
lastLetter ('abcd')
*/

